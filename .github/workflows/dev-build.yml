name: Build and Deploy Dev Environment

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Debug Git Status and Remote
        run: |
          git status
          git remote -v
          ls -la
          pwd  # Print working directory for clarity
          echo ${{ secrets.PAT_TOKEN }}

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install Sass globally
        run: npm install -g sass    

      - name: Install dependencies in dev folder
        working-directory: dev
        run: npm install
  

      - name: Create dev/common folder
        run: mkdir -p dev/common

      - name: Extract function to dev/common folder
        run: |
          sed -n '/function clarion_language_definition/,/api\.registerHighlightJSLanguage/p' common/head_tag.html | sed '$d' > dev/common/clarion-language.js

      - name: List dev/common folder contents
        run: ls -la dev/common

      - name: List common folder contents
        run: ls -la common
     
      - name: Compile SCSS
        run: npm run build-scss  

      - name: Commit and push changes with Debugging
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "GitHub Actions"
          git add dev/common/clarion-language.js
          git commit -m "Automated extraction"
          git push "https://x-access-token:${{ secrets.PAT_TOKEN }}@github.com/msarson/discourse-highlightjs-clarion.git" HEAD:master
        continue-on-error: true  # Prevent immediate failure, see what happens
